---
topic: Consumer review intelligence for P&G haircare brands: EDA + cited RAG over one review corpus
updated: 2026-09-16T11:28
---

- (note) Source: PLAN.md (rich, structured). Mode: direct extract, no elicitation. Slug review-intelligence set by user.
- (direction) Repo name is consumer-review-rag, not pg-review-intelligence (user, 2026-09-15).
- (direction) License MIT, Kent Joshua P. Alvarez 2026 (user, 2026-09-15).
- (decision) Why: portfolio project for P&G AI Engineering Intern application; grounded in P&G brands so reviewers read it as built for P&G; pairs Data Analyst layer (EDA) with AI Engineering layer (cited RAG).
- (capability) CAP-1 Reproducible cleaned review corpus in target schema for 2-3 P&G brands, with data quality report. Success: script yields data/processed/reviews_clean.parquet; report shows total rows, rows per brand, date range, percent dropped with reasons.
- (capability) CAP-2 EDA of ratings, volume, sentiment and themes per brand with written insight per chart. Success: notebooks/01_eda.ipynb runs top to bottom clean, produces every chart in eda-charts.md, each with 1-2 sentence takeaway.
- (capability) CAP-3 Question retrieves top-k relevant reviews, optionally filtered by brand and rating band. Success: 5-10 hand-tested queries return relevant reviews; filters restrict results; persisted index loads without re-embedding.
- (capability) CAP-4 Answers grounded only in retrieved reviews, every answer cites source reviews (brand, rating, snippet), declines when reviews do not cover the question. Success: answers carry citations mapping to real rows; unanswerable returns refusal; empty retrieval and rate limits handled without crash.
- (capability) CAP-5 Scored evaluation on 15-20 hand-written gold questions: retrieval hit rate, faithfulness pass/fail, refusal. Success: src/evaluate.py prints metrics; eval/results.md holds numbers plus commentary on failures.
- (capability) CAP-6 Public live demo showing both layers. Success: Streamlit Community Cloud URL works in a fresh browser with query box, brand/rating filters, cited answer, expandable sources, 1-2 EDA charts.
- (capability) CAP-7 Application-ready documentation. Success: README follows documentation.md outline incl. architecture diagram, eval numbers, limitations; paragraph mapping project to P&G JD language drafted.
- (constraint) One-week timebox (week of 2026-09-15). If a day slips protect: evaluation and limitations > deployment > extra EDA charts > extra brands.
- (constraint) P&G brands only, same category preferred; brand set finalized only after inspecting dataset availability.
- (constraint) Public existing dataset first; scraping only if public data fails, time-boxed to half a day max.
- (constraint) Zero-cost stack: Groq free tier (30 req/min, needs backoff), local embeddings with no second API key, Streamlit Community Cloud hosting.
- (constraint) Python 3.11; avoid 3.13 (ML wheel gaps, Streamlit Cloud support).
- (constraint) GROQ_API_KEY never committed: local .env (git-ignored) and Streamlit Secrets; .env.example documents the name.
- (constraint) Pipeline logic lives importable in src/; notebook and app call the same functions; retrieval logic never duplicated in the app.
- (constraint) Groq model string defined once in src/config.py; confirm current string before coding.
- (constraint) Original review text kept intact for display and citation; normalized text stored separately for embedding.
- (constraint) Limitations are a first-class deliverable: report measured numbers, never claim it works without evidence.
- (constraint) No code or data from Naya or MamaBot.
- (decision) Non-goals: multi-step agentic tool use, fine-tuning, custom-trained sentiment model, production-readiness claims, scraper-first sourcing, statistical generalization to the customer population.
- (decision) Stretch goals only after Definition of Done: agentic brand/rating routing and compare-brands tool, lightweight topic modeling, LLM-grader faithfulness check.
- (decision) Success signal: reviewer opens public URL, asks e.g. what 1-star Pantene reviewers complain about, gets answer citing real reviews; repo shows clean-running EDA notebook and eval/results.md with measured hit rate, faithfulness, refusal incl. failures.
- (decision) Stack: Groq Llama instruct (low temperature), sentence-transformers all-MiniLM-L6-v2 local (384-dim, CPU/MPS), ChromaDB persistent local, Streamlit, python-dotenv, VADER, pandas/seaborn/scikit-learn. Companion stack.md.
- (decision) Retrieval: embed reviews whole, split only very long ones; metadata brand, rating, product_name, review_date, review_id; start k=6, tune 5-8. Companion rag-design.md.
- (decision) Cleaning: drop empty review_text; dedup on review_id and exact review_text; normalize brands; keep undated bucket; save parquet. Companion data-schema.md.
- (assumption) Working brand set Head & Shoulders, Pantene, Herbal Essences; fallback Gillette/Oral-B or Olay if haircare coverage is thin.
- (assumption) Amazon Reviews Beauty/Personal Care subsets are the most likely source of P&G-brand rows.
- (assumption) Apple Silicon MacBook; embeddings on CPU or MPS, no GPU.
- (question) Which dataset is used, and does it clear the volume bar (a few hundred, ideally 1000+ reviews per brand)?
- (question) Final brand set after inspecting the data?
- (question) Current Groq Llama instruct model string?
- (question) Near-duplicate rule beyond exact-text dedup (PLAN: decide in Phase 1)?
- (question) Length threshold above which a review is split into chunks?
- (event) Rendered SPEC.md (7 CAPs) with companions stack, data-schema, eda-charts, rag-design, evaluation, documentation, architecture-diagrams, delivery-phases.
- (event) Self-validate pass 1 coherence: PASS. All CAPs have intent+success; constraints bend design; non-goals explicit; success signal demonstrable; IDs unique.
- (event) Self-validate pass 2 preservation: PASS. Wrapper-only content dropped: section 3 confirm-before-Day-1 framing, section 9 closing data-leakage anecdote.
- (event) Story Breakdown offered; proposed 7 stories, one per PLAN phase (Phase 0 scaffold already done).
- (decision) Story 1: dataset discovery, brand lock, Groq smoke test, README problem statement (open questions 1-3). spec_checkpoint true, done_checkpoint true (user).
- (decision) Story 2: download + cleaning pipeline with quality report (CAP-1). spec_checkpoint true, done_checkpoint true (user).
- (decision) Story 3: EDA notebook with insights (CAP-2). spec_checkpoint true, done_checkpoint false (user).
- (decision) Story 4: index + filtered retrieval (CAP-3). spec_checkpoint true, done_checkpoint false (user).
- (decision) Story 5: grounded cited generation (CAP-4). spec_checkpoint true, done_checkpoint true (user).
- (decision) Story 6: evaluation gold set, metrics, results write-up (CAP-5). spec_checkpoint true, done_checkpoint true (user).
- (decision) Story 7: Streamlit app, Cloud deploy, README + limitations + application paragraph (CAP-6, CAP-7). spec_checkpoint false, done_checkpoint false (user).
- (decision) invoke_dev_with notes (user accepted): story 1 scaffold exists do not recreate; story 4 test retrieval in isolation, no generation; story 7 pin requirements.txt from .venv before deploy.
- (event) stories.yaml validity check: PASS (7 entries, required fields present, string ids unique and prefix-free, no status field).
- (decision) Dataset locked (2026-09-15, maintainer + story 1): Amazon Reviews 2023, HF repo McAuley-Lab/Amazon-Reviews-2023, category Beauty_and_Personal_Care (Hou et al. 2024, arXiv:2403.03952). Resolves the which-dataset question. All_Beauty rejected as too thin (13/70/34 matching products) for per-brand trends.
- (decision) Brand set locked (2026-09-15, maintainer + story 1): Head & Shoulders, Pantene, Herbal Essences. Supersedes the working-set assumption; fallback categories not needed. Measured written reviews pre-cleaning: 9,868 / 29,670 / 16,325, total 55,863, clearing the 1,000-per-brand bar by 10x-30x. Resolves the final-brand-set question.
- (decision) Generation model locked (2026-09-15, maintainer + story 1): Groq openai/gpt-oss-120b at temperature 0.1, defined once as GROQ_MODEL in src/config.py. Supersedes the earlier Groq Llama instruct stack decision: Groq shut down its Llama chat models on 2026-08-16 and names this as the replacement. Resolves the Groq model string question.
- (constraint) openai/gpt-oss-120b is a reasoning model: its reasoning tokens count against the free-tier 8K tokens/min cap (121 on a trivial selection call, 53 of 66 completion tokens in the smoke test). Generation must budget for this alongside the 30 requests/min limit.
- (constraint) Source files are 11 GB (reviews) and 2.8 GB (metadata), so data/download_data.py streams and filters to the brand set rather than storing either file whole. Reviews join to metadata on parent_asin; the brand lives in the metadata store field.
- (constraint) Amazon Reviews 2023 publishes no terms of use for the data itself (checked: HF card, project site, MIT-licensed code repo). The project cites Hou et al. 2024 and does not redistribute raw files; this is listed under limitations.
- (direction) Two open questions stay open by maintainer direction (2026-09-16): the near-duplicate rule beyond exact-text dedup is owned by the cleaning story, and the long-review chunk split threshold by the index/retrieval story.
- (decision) data-schema.md sourcing section re-derived: the Kaggle-search / drop-to-two-brands / combine-datasets ordering is superseded by the locked source, the parent_asin join, the store-field brand rule, and the title-only match question (40/15/71 products) which the cleaning story owns. Scraping stays a documented last resort, half a day at most.
- (event) Self-validate pass 1 coherence: PASS. CAP-1..CAP-7 unchanged and each keeps intent+success; IDs stable, none renumbered or reused; the four new constraints (brand set, corpus and streaming, dataset terms, generation model and token budget) each rule something out; non-goals and success signal unchanged.
- (event) Self-validate pass 2 preservation: PASS. Three open questions resolved into constraints, two brand/dataset assumptions promoted to locked constraints, and the half-day scraping timebox relocated from SPEC constraints to data-schema.md rather than dropped. No load-bearing claim lost.
- (event) stories.yaml checked against the updated spec: no story description drifted (none names the model string or an undecided dataset), so story definitions are unchanged and no Story Breakdown re-run is needed.
